# Jyotish Engine — Dependencies
# Install with: pip install -r requirements.txt

# Core Swiss Ephemeris (sub-arcsecond precision astronomical calculations)
pyswisseph>=2.10.3

# Web framework
Flask>=3.0.0

# PyJHora core deps
numpy>=1.26.0
python-dateutil>=2.9.0
requests>=2.31.0

# Optional: needed for place/timezone lookup from city name
# (not required if you enter lat/lon/timezone directly)
pytz>=2024.1
timezonefinder>=6.5.0
geocoder>=1.38.1
geopy>=2.4.1
